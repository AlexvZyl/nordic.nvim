local c = require 'nordic.colors'

return {

    DiffviewNormal = {
        bg = c.bg_dark,
        fg = c.fg
    },

    DiffviewFilePanelTitle = {
        fg = c.orange.base,
        bold = true
    },

    DiffviewFolderSign = {
        fg = c.yellow.dim
    },

    DiffviewFolderName = {
        fg = c.blue1
    },

    DiffviewStatusLine = {
        bg = c.black
    }

}
